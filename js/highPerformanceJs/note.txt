fuck

文档对象模型DOM。

浏览器的JS引擎受不住了。V8诞生，但是还是要关注性能。

第一章 加载和执行
多数浏览器使用单一进程来处理用户界面（UI）刷新和JavaScript脚本执行，所以同一时刻只能做一件事。执行JS的过程中，页面渲染和用户交互完全被阻塞。

<script> 加载外链JavaScript文件，紧挨着的<link> 标签用来加载外部CSS，或其他页面元信息。
<script> 下载外部资源不会阻塞其他<script>标签。但会阻塞其他资源下载，如图片。

方法：将<script>标签按情况放到底部
